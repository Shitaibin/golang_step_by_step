syntax = "proto3";
option go_package = ".;main";

package main;

message String { string value = 1; }

service HelloService {
  rpc Hello(String) returns (String);
  rpc HiStream(stream String) returns (stream String);
}